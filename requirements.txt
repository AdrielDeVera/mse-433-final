# MSE 433 — Demand Forecasting & Inventory Optimisation
# Core dependencies (pinned to tested versions)

numpy==2.2.6
pandas==2.3.3
matplotlib==3.10.8
seaborn==0.13.2

# Dashboard (install separately if needed)
streamlit==1.45.0
